
class DBHelper:

    def sql_query(db,query):
        cursor = db.cursor()
        db.execute(query)
        row = cursor.fetchone() 
        while row: 
            print(row[0])
            row = cursor.fetchone()
